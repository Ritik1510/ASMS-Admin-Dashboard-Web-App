import { createHttpClient } from "../../http/client/createHttpClient.http";

/**
 * It can be used to test the API routes, 
 * which is completely public routes 
 * */
export const publicApi = createHttpClient();